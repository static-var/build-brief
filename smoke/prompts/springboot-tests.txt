Without editing files, first run the project's `clean` task, then run the tests in this Spring Boot project and briefly summarize whether the application context and service tests passed. Include the exact command or commands you used.
